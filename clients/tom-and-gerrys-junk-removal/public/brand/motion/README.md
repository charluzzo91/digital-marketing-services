Motion assets. Currently EMPTY BY DESIGN — the loader is coded SVG + Framer Motion
(`src/components/IntroOverlay.tsx`), which costs 0 credits and cannot become the LCP element.
Only add video here if a coded version is proven insufficient. If you do: WebM + MP4 fallback,
explicit dimensions, optimised poster, aggressive size cap.
