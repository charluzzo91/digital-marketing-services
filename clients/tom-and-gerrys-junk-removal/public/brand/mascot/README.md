Shipped mascot assets. SVG preferred; WebP/AVIF only for raster fallbacks.
Currently the mascot is coded in `src/components/Mascot.tsx` — replace those paths
with the vectorised approved character, then drop any raster exports here.
